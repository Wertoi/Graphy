<UserControl x:Class="Graphy.View.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Graphy"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="500">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="15,15,0,15">
            
            <!-- BACK BUTTON -->
            <Button x:Name="BackButton" Style="{StaticResource NormalButton}" Click="BackButton_Click">
                <Viewbox Style="{StaticResource IconViewBox}">
                    <Canvas Style="{StaticResource IconCanvas}">
                        <Path Style="{StaticResource BackIcon}"/>
                    </Canvas>
                </Viewbox>
            </Button>

            <TextBlock Text="{local:Loc Settings_Title}" FontSize="22" Foreground="{StaticResource PrimaryColorDark}" VerticalAlignment="Center" Margin="20,0,0,0"/>

        </StackPanel>
        
        <ScrollViewer Grid.Row="1" Margin="15" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" Grid.IsSharedSizeScope="True">

                <TextBlock Text="{local:Loc Settings_General_Title}" FontSize="14" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,5"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumnGroup"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="InputFieldColumnGroup"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="1" Text="{local:Loc Settings_General_LanguageLabel}" Style="{StaticResource LabelFormTextBlock}"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" VerticalContentAlignment="Center" Padding="5,8,15,8"
                              ItemsSource="{Binding Source={StaticResource Locator}, Path=SettingViewModel.SelectedLanguage, Converter={StaticResource EnumToCollectionConverter}}"
                              SelectedItem="{Binding Source={StaticResource Locator}, Path=SettingViewModel.SelectedLanguage, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource LocalizedEnumToStringConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>

                <TextBlock Grid.Row="2" Text="{local:Loc Settings_FontManagement_Title}" FontSize="14" Foreground="{StaticResource PrimaryColor}" Grid.ColumnSpan="3" Margin="0,20,0,5"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="LabelColumnGroup"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="InputFieldColumnGroup"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="{local:Loc Settings_FontManagement_3DFontDirectoryLabel}" Style="{StaticResource LabelFormTextBlock}" />

                    <TextBox Grid.Column="1" Style="{StaticResource ReadOnlyTextBox}"
                             Text="{Binding Source={StaticResource Locator}, Path=SettingViewModel.GeneratedFontDirectoryPath}" />

                    <Button Grid.Column="2" Style="{StaticResource NormalButton}"
                            Command="{Binding Source={StaticResource Locator}, Path=SettingViewModel.SelectGeneratedFontDirectoryPathCommand}"
                            CommandParameter="{Binding Source={StaticResource Locator}, Path=SettingViewModel.GeneratedFontDirectoryPath}">
                        <Viewbox Style="{StaticResource IconViewBox}">
                            <Canvas Style="{StaticResource IconCanvas}">
                                <Path Style="{StaticResource OpenFolderIcon}"/>
                            </Canvas>
                        </Viewbox>
                    </Button>

                </Grid>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{local:Loc Settings_AddNewFont}" VerticalAlignment="Center" Style="{StaticResource LabelFormTextBlock}"/>
                    <Button x:Name="AddNewFontButton" Style="{StaticResource NormalButton}" Click="AddNewFontButton_Click"
                            IsEnabled="{Binding Source={StaticResource Locator},Path=FontViewModel.IsClassicFontCollectionEmpty, Converter={StaticResource InvertBooleanConverter}}">
                        <Viewbox Style="{StaticResource IconViewBox}">
                            <Canvas Style="{StaticResource IconCanvas}">
                                <Path Style="{StaticResource AddIcon}"/>
                            </Canvas>
                        </Viewbox>
                    </Button>
                </StackPanel>


                <Rectangle Height="1" Width="150" HorizontalAlignment="Left" Margin="0,25,0,20" Fill="{StaticResource Gray217}"/>

                <TextBlock Grid.Row="2" Text="{local:Loc Settings_About_Title}" FontSize="14" Foreground="{StaticResource PrimaryColor}" Grid.ColumnSpan="3" Margin="0,0,0,5"/>

                <TextBlock TextWrapping="Wrap" Width="350" HorizontalAlignment="Left" TextAlignment="Justify">
                    <Run Text="Graphy" Foreground="{StaticResource SecondaryColor}"/>
                    <Run Text="is a completly open source and free software which allows you to write 3D texts easily in Catia V5."/>
                    <LineBreak/>
                    <LineBreak/>
                    <Run Text="Develop by :"/>
                    <LineBreak/>
                    <Run Text="- Guillaume ANDRIEU" Foreground="#FFA6A6A6"/>
                    <LineBreak/>
                    <Run Text="- Xavier BETBEDER" Foreground="#FFA6A6A6"/>
                </TextBlock>

            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="15,0,15,10">
            <TextBlock HorizontalAlignment="Left" Text="© Graphy, 2019"/>
            <TextBlock HorizontalAlignment="Right">
                <Hyperlink Command="{Binding Source={StaticResource Locator}, Path=SettingViewModel.ShowLicenceCommand}">
                    <TextBlock Text="{local:Loc Settings_About_Licenses}"/>
                </Hyperlink>
            </TextBlock>
        </Grid>

    </Grid>
</UserControl>
